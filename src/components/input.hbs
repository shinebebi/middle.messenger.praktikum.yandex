<div class="input_container">
    <p class="field-name {{name}}-field-name"></p>
    <input placeholder="{{name}}" class="input {{name}}" minlength="2" maxlength="40" name="{{name}}" required/>
    <span class="{{name}}-error"></span>
</div>